@model List<Order>
@{
    string status = "";
}
<div class="col-md-8 grid-margin stretch-card">
    <div class="card">
        <div class="card-body">
            <h4 class="card-title">
                <i class="fas fa-table"></i>
                Son Siparişler
            </h4>
            <div class="table-responsive">
                <table class="table">
                    <thead>
                        <tr>
                            <th>Müşteri</th>
                            <th>Ürün</th>
                            <th>Adet</th>
                            <th>Durum</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach(var order in Model)
                        {
                            <tr>
                                <td class="font-weight-bold">
                                    @order.Customer.CustomerName @order.Customer.CustomerSurname
                                </td>
                                <td class="text-muted">
                                    @order.Product.ProductName
                                </td>
                                <td>
                                    @order.OrderCount
                                </td>
                                @if (order.Status == "Taşıma Durumunda")
                                {
                                    <td>
                                        <label class="badge badge-outline-primary badge-pill">@order.Status</label>
                                    </td>
                                }
                                else if (order.Status == "Teslim Edildi")
                                {
                                    <td>
                                        <label class="badge badge-outline-success badge-pill">@order.Status</label>
                                    </td>
                                }
                                else if (order.Status == "Sipariş Alındı")
                                {
                                    <td>
                                        <label class="badge badge-outline-info badge-pill">@order.Status</label>
                                    </td>
                                }
                                else if (order.Status == "Ödeme Bekleniyor")
                                {
                                    <td>
                                        <label class="badge badge-outline-warning badge-pill">@order.Status</label>
                                    </td>
                                }
                                else if (order.Status == "İptal Edildi")
                                {
                                    <td>
                                        <label class="badge badge-outline-danger badge-pill">@order.Status</label>
                                    </td>
                                }
                                else
                                {
                                    <td>
                                        <label class="badge badge-secondary badge-pill">@order.Status</label>
                                    </td>
                                }

                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

<div class="col-md-4 grid-margin stretch-card">
    <div class="card">
        <div class="card-body">
            <h4 class="card-title">
                <i class="fas fa-calendar-alt"></i>
                Takvim
            </h4>
            <div id="inline-datepicker-example" class="datepicker"></div>
        </div>
    </div>
</div>
