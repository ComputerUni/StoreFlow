@model List<Todo>

<li class="nav-item dropdown">
    <a class="nav-link count-indicator dropdown-toggle" id="notificationDropdown" href="#" data-toggle="dropdown">
        <i class="fas fa-bell mx-0"></i>
        <span class="count">@ViewBag.todoTotalCount</span>
    </a>
    <div class="dropdown-menu dropdown-menu-right navbar-dropdown preview-list" aria-labelledby="notificationDropdown">
        <a href="/Todo/Index" class="dropdown-item">
            <p class="mb-0 font-weight-normal float-left">
                Toplam @ViewBag.todoTotalCount aktif bildiriminiz var
            </p>
            <span class="badge badge-pill badge-warning float-right">Tümünü Gör</span>
        </a>
        <div class="dropdown-divider"></div>
        @foreach(var todo in Model)
        {
            string badgeColorClass = todo.Priority switch
            {
                "Birincil" => "bg-danger",
                "İkincil" => "bg-warning",
                "Üçüncül" => "bg-info",
                "Dördüncül" => "bg-primary",
                "_" => "bg-inverse-secondary",

            };

            <a class="dropdown-item preview-item">
                <div class="preview-thumbnail">
                    <div class="preview-icon @badgeColorClass">
                        <i class="fas fa-exclamation-circle mx-0"></i>
                    </div>
                </div>
                <div class="preview-item-content">
                    <h6 class="preview-subject font-weight-medium bg-">@todo.Description</h6>
                    <p class="font-weight-light small-text">
                        @todo.Priority
                    </p>
                </div>
            </a>
            <div class="dropdown-divider"></div>
        } 
    </div>
</li>
