@model List<string>
@{
    ViewData["Title"] = "IncompleteTask";
    Layout = "~/Views/Layout/Index.cshtml";
    int count = 0;
}


<div class="row">
    <div class="col-lg-12 grid-margin stretch-card">
        <div class="card">
            <div class="card-body">
                <h4 class="card-title font-weight-bold">Append ile Gün Sonu Kontrolü</h4>
                <p class="card-description">
                    <code>Kategori ile ilgili işlemlerinizi buradan yapabilirsiniz.</code>
                </p>
                <div class="table-responsive">
                    <table class="table">
                        <thead>
                            <tr>
                                <th>#</th>
                                <th>Açıklama</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                count++;
                                <tr>
                                    <td>@count</td>
                                    <td>@item</td>
                                </tr>
                            }
                        </tbody>
                    </table>
                    <a href="/Category/AddCategory/" class="btn btn-outline-info"><i class="fa fa-plus"></i>   Yeni Kategori</a>
                </div>
            </div>
        </div>
    </div>
</div>