﻿@using X.PagedList.Mvc.Core
@model X.PagedList.IPagedList<Message>

@{
    ViewData["Title"] = "MessageList";
    Layout = "~/Views/Layout/Index.cshtml";
    int count = 0;
}


<div class="row">
    <div class="col-lg-12 grid-margin stretch-card">
        <div class="card">
            <div class="card-body">
                <h4 class="card-title font-weight-bold">Mesajlar</h4>
                <p class="card-description">
                    <code>Mesaj ile ilgili işlemlerinizi buradan yapabilirsiniz.</code>
                </p>
                <div class="table-responsive">
                    <table class="table">
                        <thead>
                            <tr>
                                <th>#</th>
                                <th>Gönderen</th>
                                <th>Konu</th>
                                <th>Durum</th>
                                <th>Tarih</th>
                                <th>İşlemler</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                count++;
                                <tr>
                                    <td>@count</td>
                                    <td>@item.SenderNameSurname</td>
                                    <td>@item.MessageTitle</td>
                                    <td>@item.IsRead</td>
                                    <td>@item.Datetime</td>
                                    <td style="display: flex; gap: 8px;">
                                        <a href="/Message/DeleteMessage/@item.MessageId" class="btn btn-outline-danger"><i class="fa fa-trash"></i></a>
                                        <a href="/Message/UpdateMessage/@item.MessageId" class="btn btn-outline-warning"><i class="fa fa-pencil-alt"></i></a>
                                        <a href="/Message/UpdateMessage/@item.MessageId" class="btn btn-outline-info"><i class="fa fa-eye"></i></a>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                    <a href="/Message/CreateMessage/" class="btn btn-outline-info"><i class="fa fa-plus"></i>   Yeni Mesaj</a>
                </div>
                <div class="d-flex align-items-center justify-content-end mt-4 pt-3 border-top">
                    @Html.PagedListPager(Model, page => Url.Action("MessageList", new { page }), new PagedListRenderOptions
                    {
                        UlElementClasses = new[] { "pagination", "mb-0" },
                        LiElementClasses = new[] { "page-item" },
                        PageClasses = new[] { "page-link" }
                    })
                </div>
            </div>
        </div>
    </div>
</div>
